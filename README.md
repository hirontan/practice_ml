# practice_ml

## インストールしておくパッケージ
```
$ pip install numpy
$ pip install pandas
$ pip install matplotlib
$ pip install scikit-learn
$ pip install seaborn
```

## Jupyter Notebook の起動
```
$ jupyter notebook
```
